#!/usr/bin/env bash
#MISE description="Build the gh extension binary"
#MISE alias="b"
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

go build -o gh-ext-issue-sync .
echo "Built gh-ext-issue-sync"
